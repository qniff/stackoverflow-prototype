<UserControl x:Class="PrototypeDemo.CommentForm"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PrototypeDemo"
             mc:Ignorable="d" d:DesignWidth="800" Height="100.545">
    <Grid Background="Azure" >

        <Grid.RowDefinitions>
            <RowDefinition Height= "1*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width ="4*"/>
        </Grid.ColumnDefinitions>
        <Label x:Name="lbText" Content="Text"  HorizontalAlignment="Left" VerticalAlignment="Center" Margin="0,38,0,37" Width="600"/>
        <Label x:Name="lbAuthor" Content="Author"  HorizontalAlignment="Center" VerticalAlignment="Center" Margin="619,38,111,37" Width="70"/>
        <Label x:Name="lbDate" Content="Date"  HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,38,0,37" Width="91"/>
    </Grid>
</UserControl>
